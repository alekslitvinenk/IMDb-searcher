imdb = {
  connectionPool      = "HikariCP"
  numThreads = 50
  url                 = "jdbc:mysql://localhost:3306/imdb?useSSL=false"
  properties = {
    databaseName = "imdb"
    user = "root"
    password = "jobjob"
  }
  keepAliveConnection = true
}